import TeacherAssignments from "@/components/teacher/AssignmentsView";

export const metadata = { title: "Assignments", description: "Create assignments from published tests and track every submission." };

export default function TeacherAssignmentsPage() {
  return <TeacherAssignments />;
}
