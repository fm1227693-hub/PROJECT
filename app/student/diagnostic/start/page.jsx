import { DiagnosticSetup } from "@/components/test/DiagnosticFlow";

export const metadata = { title: "Start a diagnostic" };

export default function DiagnosticStartPage() {
  return <DiagnosticSetup />;
}
