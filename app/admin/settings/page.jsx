import { AdminSettings } from "@/components/admin/AdminContent";

export const metadata = { title: "Settings", description: "Registration rules, support contacts and demo data reset." };

export default function Page() {
  return <AdminSettings />;
}
