import { AdminFaq } from "@/components/admin/AdminContent";

export const metadata = { title: "FAQ manager", description: "Edit the public help centre with live reordering." };

export default function Page() {
  return <AdminFaq />;
}
