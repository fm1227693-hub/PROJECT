import { AdminTests } from "@/components/admin/AdminContent";

export const metadata = { title: "Tests", description: "Build, publish and unpublish the platform test papers." };

export default function Page() {
  return <AdminTests />;
}
