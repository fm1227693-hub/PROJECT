import { AdminClasses } from "@/components/admin/AdminViews";

export const metadata = { title: "Classes", description: "Every class on the platform with rosters and assigned teachers." };

export default function Page() {
  return <AdminClasses />;
}
