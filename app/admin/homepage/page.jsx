import { AdminHomepage } from "@/components/admin/AdminContent";

export const metadata = { title: "Homepage", description: "Edit the hero and choose which sections render." };

export default function Page() {
  return <AdminHomepage />;
}
