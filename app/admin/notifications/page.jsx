import { AdminNotifications } from "@/components/admin/AdminContent";

export const metadata = { title: "Notifications", description: "Compose role-targeted notifications and review delivery." };

export default function Page() {
  return <AdminNotifications />;
}
