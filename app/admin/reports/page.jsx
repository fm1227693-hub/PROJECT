import { AdminReports } from "@/components/admin/AdminContent";

export const metadata = { title: "Reports", description: "Generate and export the standard platform reports." };

export default function Page() {
  return <AdminReports />;
}
