import { AdminSubjects } from "@/components/admin/AdminViews";

export const metadata = { title: "Subjects & topics", description: "The skill taxonomy behind diagnostics and learning paths." };

export default function Page() {
  return <AdminSubjects />;
}
