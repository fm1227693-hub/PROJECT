import { AdminTestResults } from "@/components/admin/AdminContent";

export const metadata = { title: "Test results", description: "All diagnostic and assignment results across the platform." };

export default function Page() {
  return <AdminTestResults />;
}
