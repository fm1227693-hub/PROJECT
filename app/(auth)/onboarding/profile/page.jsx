import { OnboardingProfile } from "@/components/onboarding/OnboardingWizard";

export const metadata = { title: "Profile setup", alternates: { canonical: "/onboarding/profile" } };

export default function OnboardingProfilePage() {
  return <OnboardingProfile />;
}
