import { VerifyEmailPanel } from "@/components/auth/AuthForms";

export const metadata = { title: "Verify email", alternates: { canonical: "/verify-email" } };

export default function VerifyEmailPage() {
  return <VerifyEmailPanel />;
}
